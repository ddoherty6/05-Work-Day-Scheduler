$("#currentDay").text(moment().format("dddd, MMMM Do")); //display current day in header

    
var timeStyling = function(currentTime) {
    descriptionEls = $("textarea");

    descriptionEls.array.forEach(element => {
        
        
    });

    
}